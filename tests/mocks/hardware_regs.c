/* tests/mocks/hardware_regs.c — single definitions for extern CH4 APU register stubs.
 * Picked up automatically by MOCK_SRCS in the Makefile (tests/mocks/*.c). */
#include <stdint.h>

uint8_t NR41_REG = 0;
uint8_t NR42_REG = 0;
uint8_t NR43_REG = 0;
uint8_t NR44_REG = 0;
